# Frontend Configuration
# Backend URL - only needed for production deployment
# Development: defaults to http://localhost:8000
# Production: set to your Railway backend URL
# NEXT_PUBLIC_BACKEND_URL=https://your-app.railway.app

# Selection Limits
# Maximum number of selections allowed per image
# Leave empty or comment out for unlimited selections
# NEXT_PUBLIC_MAX_SELECTIONS=5

# Backend Configuration  
# CORS allowed origins - comma-separated list
# Development: include localhost
# Production: only your domains
# ALLOWED_ORIGINS=http://localhost:3000,https://*.vercel.app

# AI Model Selection
# Choose AI model for background removal
# Options: u2netp (fast), u2net (quality), u2net_human_seg, u2net_cloth_seg, isnet-general-use
# Default: u2netp (recommended for Railway free tier)
# MODEL_NAME=u2netp